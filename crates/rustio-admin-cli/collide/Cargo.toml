[package]
name = "collide"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"

[dependencies]
rustio-admin = "0.13.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
chrono = { version = "0.4", features = ["serde"] }
