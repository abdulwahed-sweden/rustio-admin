# FixShop — copy to `.env` and adjust.

RUST_LOG=info

DATABASE_URL=postgres://postgres:postgres@localhost:5432/fixshop_dev

# Staff accounts are created with the CLI, never from SQL — passwords
# are Argon2id-hashed by the framework and there is no plaintext at
# rest. See the README for the three accounts this example expects.
