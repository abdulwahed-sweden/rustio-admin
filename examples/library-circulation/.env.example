# Postgres connection. Override per local install.
DATABASE_URL=postgres://localhost/library_circulation_demo

# Session signing secret (32+ bytes). Generate via:
#   openssl rand -base64 48
RUSTIO_SECRET_KEY=<replace-me>
